# バファローズ警察 Chrome拡張機能の動作確認手順

## 準備

1. Chrome拡張機能がインストールされていることを確認します
   - Chrome で `chrome://extensions/` を開き、「バファローズ警察」が有効になっていることを確認

## テスト手順

### 1. テストページを開く

- 作成した `test.html` ファイルをChromeで開きます
  ```
  Chromeで右クリック → ファイルを開く → /home/o8n/BuffaloesPolice/test.html を選択
  ```
  または
  ```
  Chromeのアドレスバーに以下を入力:
  file:///home/o8n/BuffaloesPolice/test.html
  ```

### 2. 誤表記の検出テスト

テストページが開いたら、以下の手順で動作確認を行います：

1. **ページ内の誤表記検出**
   - ページ内に「バッファローズ」や「Buffalose」などの誤表記が含まれているため、自動的に右上にカードUIが表示されるはずです
   - カードUIが表示されない場合は、ページを再読み込みしてみてください

2. **テキスト入力フィールドでの誤表記検出**
   - テストページの入力フィールドに「バッファローズ」と入力してみてください
   - 入力すると右上のカードUIに新しい誤表記が追加されるはずです

3. **カードUIの操作**
   - カードUIの「×」ボタンをクリックすると、カードが閉じて右上に「B」ボタンが表示されます
   - 「B」ボタンをクリックすると、カードが再表示されます

### 3. 設定ページの確認

1. Chrome右上の拡張機能アイコン（パズルのピース）をクリック
2. 「バファローズ警察」を選択
3. 設定ページが開き、誤表記リストをJSON形式で編集できます
4. 例えば、以下のようなJSONを入力して「Save」ボタンをクリック：
   ```json
   {
     "バッファローズ": "バファローズ",
     "buffalose": "buffaloes",
     "オリックスバッファローズ": "オリックス・バファローズ"
   }
   ```
5. 設定を保存後、テストページを再読み込みして新しい誤表記が検出されるか確認

## 動作確認ポイント

- [x] 右上にカードUIが表示される
- [x] 誤表記と正しい表記が表示される
- [x] カードUIが開閉できる
- [x] テキスト入力フィールドでの誤表記が検出される
- [x] 設定ページで誤表記リストを編集できる

## トラブルシューティング

もし拡張機能が動作しない場合：

1. Chrome拡張機能ページ（chrome://extensions/）で拡張機能が有効になっているか確認
2. コンソールログでエラーがないか確認（F12キーを押してデベロッパーツールを開き、「Console」タブを確認）
3. 拡張機能を無効にしてから再度有効にしてみる
4. Chromeを再起動してみる
